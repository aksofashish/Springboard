# JSON Exercise
I was provided with a JSON file having 500 records in JSON format. 
I have to familiarize with the Python JSON functions to work with them efficiently. 
Apart from that I will be doing some analysis like imputing missing data on JSON file.  
